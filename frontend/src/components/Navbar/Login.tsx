import $Login from "../../styles/Navbar/Login";

const Login: React.FC = (): JSX.Element => {
  return (
    <$Login>
      <span>Welcome, CupLover420!</span>&nbsp;
      <button>Logout</button>
    </$Login>
  );
};

export default Login;
